'use strict';

var generatePlayerBoar = function generatePlayerBoar(numberOfRows, numberOfColumns) {
    var board = [];

    for (var row = 0; row < numberOfRows.length; rows++) {
        row = [];
        for (var column = 0; column < numberOfColumns.length; column++) {
            column = [];
            row.push(' ');
        }
    }
};